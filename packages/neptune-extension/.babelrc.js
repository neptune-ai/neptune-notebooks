const config = {
  'presets': [
    '@babel/preset-env',
    '@babel/preset-react',
    '@babel/preset-typescript',
  ],
  'plugins': [
    '@babel/plugin-proposal-object-rest-spread',
  ],
};

module.exports = config;
